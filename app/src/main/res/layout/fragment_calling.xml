<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".calling">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:textAlignment="center"
        >

        <EditText
            android:id="@+id/fragment_calling_editText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:textSize="40dp"
            android:background="@drawable/fragment_text_input"
            android:layout_margin="10dp"
            android:inputType="phone" />

        <Button
            android:id="@+id/fragment_calling_button"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:background="@drawable/fragment_button"
            android:layout_margin="10dp"
            android:layout_marginEnd="20dp"
            android:layout_gravity="center"
            android:text="CALL" />
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fragment_calling_recycleview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="20dp"
            android:layout_margin="10dp"
            />
    </LinearLayout>

</FrameLayout>